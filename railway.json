{
  "build": {
    "use": "@railway/php",
    "variables": {
      "NODE_VERSION": "18"
    },
    "commands": [
      "composer install --no-interaction --prefer-dist --optimize-autoloader",
      "npm install",
      "npm run build",
      "php artisan storage:link",
      "chmod -R 775 storage bootstrap/cache"
    ]
  },
  "deploy": {
    "startCommand": "php artisan config:clear && php artisan cache:clear && php artisan route:clear && php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=${PORT}"
  }
}